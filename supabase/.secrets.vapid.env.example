# Copy to .secrets.vapid.env (gitignored), fill in, then:
#   npx supabase secrets set --env-file supabase/.secrets.vapid.env --project-ref <ref>
# Must match the same pair as Vercel PWA_VAPID_PUBLIC_KEY (public) + your private key.

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
